import React from 'react';
import { View, Text, ScrollView } from 'react-native';

const PrimaryBenefits = ({ navigation }) => {
  return (
    <ScrollView>
      <Text>Primary Benefits</Text>
    </ScrollView>
  );
};

export default PrimaryBenefits;
