import React from 'react';
import { View, Text, ScrollView } from 'react-native';

const SupplementaryBenefits = ({ navigation }) => {
  return (
    <ScrollView>
      <Text>Supplementary Benefits</Text>
    </ScrollView>
  );
};

export default SupplementaryBenefits;
